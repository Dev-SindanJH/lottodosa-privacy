# 로또도사 — 개인정보 처리방침 배포 가이드

## 1. GitHub Pages로 공개 URL 만들기 (약 5분, 무료)

구글 플레이는 **누구나 로그인 없이 접근 가능한 URL**을 요구합니다. 구글 드라이브 공유링크, 노션 비공개 페이지, PDF 다운로드 링크는 반려 사유가 될 수 있습니다.

### 절차

1. **GitHub 로그인** → 우측 상단 `+` → **New repository**
2. 설정값
   - Repository name: `lottodosa-privacy` (원하는 이름 가능)
   - **Public** 선택 (Private이면 페이지가 열리지 않습니다)
   - `Add a README file` 체크
   - **Create repository**
3. 저장소 화면에서 **Add file → Upload files** 클릭 → 전달드린 `index.html` 업로드 → **Commit changes**
4. 상단 **Settings** 탭 → 좌측 **Pages** 메뉴
   - Source: `Deploy from a branch`
   - Branch: `main` / 폴더 `/ (root)` → **Save**
5. 1~2분 후 Pages 화면 상단에 URL이 표시됩니다.

```
https://<본인깃허브아이디>.github.io/lottodosa-privacy/
```

6. **시크릿 창에서 해당 URL을 열어** 정상적으로 보이는지 확인 (로그인 없이 열려야 함)

> 파일명은 반드시 `index.html`이어야 위 주소로 바로 열립니다.

---

## 2. Play Console에 등록할 위치 (3곳)

| 위치 | 경로 | 입력 내용 |
|---|---|---|
| ① 앱 콘텐츠 | 앱 콘텐츠 → **개인정보처리방침** → 수정 | 위 URL |
| ② 스토어 등록정보 | 스토어 설정 → 스토어 등록정보 → 개인정보처리방침 | 위 URL (①과 동일) |
| ③ 데이터 보안 | 앱 콘텐츠 → **데이터 보안** | 아래 3번 표대로 응답 |

---

## 3. 데이터 보안(Data safety) 설문 응답표

이 앱의 실제 코드(AdMob, geolocator, mobile_scanner, shared_preferences, http, in_app_purchase, flutter_local_notifications)를 기준으로 정리했습니다. **방침 내용과 이 응답이 불일치하면 반려됩니다.**

### 개요 질문

| 질문 | 답변 |
|---|---|
| 앱이 사용자 데이터를 수집하거나 공유하나요? | **예** (광고 SDK 때문에 '아니요'로 답하면 안 됩니다) |
| 전송 중 데이터가 암호화되나요? | **예** |
| 사용자가 데이터 삭제를 요청할 수 있나요? | **예** (앱 삭제·데이터 삭제로 즉시 삭제 가능) |
| Play Families 정책 대상인가요? | **아니요** (아동 대상 앱 아님) |

### 수집·공유 데이터 유형

| 데이터 유형 | 수집 | 공유 | 목적 | 필수/선택 |
|---|---|---|---|---|
| **위치 → 대략적인 위치** | 예 | 예 (광고 제공업체) | 광고 또는 마케팅 | 필수 |
| **위치 → 정확한 위치** | 예 | 아니요 | 앱 기능 (주변 판매점 검색) | 선택 (사용자가 거부 가능) |
| **앱 활동 → 앱 내 검색어** | 예 | 예 (지도 API) | 앱 기능 | 선택 |
| **기기 또는 기타 ID (광고 ID)** | 예 | 예 | 광고 또는 마케팅, 분석 | 필수 |

> **인앱결제(금융 정보 → 구매 내역):** 결제는 Google Play 결제 시스템이 처리하고 앱은 구매 결과(상품 ID·이용 기간)만 기기에 저장할 뿐 개발자 서버로 보내지 않으므로, **"금융 정보 → 구매 내역" 수집은 '아니요'** 로 답합니다. (Google 기준: Play 결제 시스템을 통한 구매를 개발자가 별도로 수집·전송하지 않으면 신고 대상이 아님. 추후 Play Developer API로 서버에서 영수증을 검증하는 구조로 바꾸면 이 답변을 재검토해야 합니다.)
> **카메라/사진:** QR 인식만 하고 이미지를 저장·전송하지 않으므로 **"사진 및 동영상" 수집은 '아니요'** 로 답합니다.
> **저장한 번호:** 기기 내부에만 저장되고 외부로 나가지 않으므로 데이터 보안에서는 신고 대상이 아닙니다 (Google 기준: on-device only는 "수집"이 아님).

### 광고 ID 선언 (Android 13+ 필수)

`google_mobile_ads` 사용 시 `com.google.android.gms.permission.AD_ID` 권한이 자동으로 병합됩니다. 데이터 보안에서 **"광고 ID 사용: 예"** 로 선언해야 합니다. 선언하지 않으면 앱이 거부됩니다.

### 앱 콘텐츠 → 광고 / 인앱 구매

- "앱에 광고가 포함되어 있나요?" → **예**
- **인앱 구매**: 광고 제거 구독(`ad_free_monthly`), 1개월권(`ad_free_1month`, `ad_free_1month_ext`), 후원 4종(`support_coffee_4th`, `support_donation_1st~3rd`)을 판매하므로 스토어 등록정보에 "인앱 구매" 배지가 표시됩니다. 상품을 Play 콘솔에 등록하면 자동 반영되며, 콘텐츠 등급 설문의 인앱 구매 문항에도 **"예"** 로 답해야 합니다.
- **구독 상품 표시 의무**: 구독은 가격·결제 주기·해지 방법을 스토어 등록정보와 앱 내 구매 화면에 모두 명시해야 합니다. (현재 스토어 화면에 "해지 전까지 매월 자동 결제 · 스토어 구독 관리에서 해지" 안내가 들어가 있습니다.)

---

## 4. 함께 점검할 항목 (반려 예방)

- [ ] **앱 이름 통일** — 현재 `AndroidManifest.xml`의 `android:label`이 `lotto_maker`입니다. 스토어명(로또도사)과 맞춰 수정하세요.
- [ ] **콘텐츠 등급 설문** — 로또 관련 앱은 "도박/복권 관련 콘텐츠" 문항에서 *실제 베팅·현금 거래 없음, 정보 제공 및 번호 생성용*임을 정확히 응답해야 합니다. 실제 복권 구매/베팅 기능이 없다는 점이 핵심입니다. **주의:** 앱에 인앱결제(광고 제거·후원)가 있지만 이는 복권 구매·베팅이 아니라 디지털 콘텐츠 결제이므로, 도박 문항은 그대로 "아니요"로 답하고 인앱 구매 문항에만 "예"로 답하면 됩니다.
- [ ] **위치 권한 사유** — 정밀 위치를 쓰므로, 앱 첫 사용 시 "주변 판매점 검색을 위해 위치 권한이 필요합니다" 같은 사전 안내 다이얼로그를 넣어두면 심사와 사용자 신뢰 모두에 유리합니다. (백그라운드 위치는 사용하지 않으므로 별도 신고 불필요)
- [ ] **`usesCleartextTraffic="true"`** — 현재 매니페스트에 평문 HTTP가 허용되어 있습니다. 확인 결과 실제 호출은 모두 HTTPS이므로, 이 옵션과 `network_security_config`가 꼭 필요하지 않다면 제거하는 편이 보안 심사에 안전합니다.
- [ ] **카카오 REST API 키가 앱에 내장되어 있음** — 개인정보 문제는 아니지만, 키가 노출되면 타인이 사용할 수 있습니다. 카카오 개발자 콘솔에서 **플랫폼(패키지명 + 키 해시) 제한**을 걸어두세요.
- [ ] **해외 배포 시** — 유럽/영국에 배포한다면 AdMob의 **UMP(User Messaging Platform) 동의 양식**을 붙여야 GDPR을 충족합니다. 한국만 배포한다면 지금 상태로 문제없습니다.

---

## 5. 앱 안에도 링크 넣기 (권장)

설정 화면에 "개인정보 처리방침" 항목을 만들고 `url_launcher`(이미 설치됨)로 위 URL을 열도록 하면 심사 시 가점 요소이며, 접근권한 고지 의무도 함께 충족됩니다.

```dart
ListTile(
  leading: const Icon(Icons.privacy_tip_outlined),
  title: const Text('개인정보 처리방침'),
  onTap: () => launchUrl(
    Uri.parse('https://<본인깃허브아이디>.github.io/lottodosa-privacy/'),
    mode: LaunchMode.externalApplication,
  ),
),
```

---

## 6. 방침 문서 수정이 필요할 때

`index.html`에서 아래 값만 바꾸면 됩니다.

- 앱 이름: `로또도사`
- 이메일: `hyuk9511@gmail.com`
- 시행일 / 최종 수정일: `2026년 8월 27일` (본문 상단 `.meta` 문단과 하단 `footer` 두 곳)

기능을 추가(예: 회원가입, 새 결제 상품, 애널리틱스·푸시 도입)하면 **1번 표와 4번 표를 반드시 갱신**하고 데이터 보안 설문도 함께 수정해야 합니다.

### 개정 이력

| 시행일 | 내용 |
|---|---|
| 2026년 7월 26일 | 최초 시행 |
| 2026년 8월 27일 | **인앱결제 반영.** 기존 "결제 기능을 제공하지 않으며"라는 문구가 사실과 달라 삭제하고, ① 1번 표에 "인앱결제 이용 내역" 행 추가 ② "인앱결제에 관한 안내" 항목 신설 ③ 4번 표에 Google Play / App Store 결제 시스템 행 추가 ④ 5번 보유기간에 구매 내역 항목 추가 ⑤ 3번 광고 항목에 "광고 제거 구매 시 광고 미노출" 문장 추가 ⑥ 영문 요약과 `delete-data.html`에도 동일 내용 반영. 개발자가 새로 수집하는 개인정보는 없으므로 **데이터 보안 설문 응답은 변경 없음** |

> **개정 후 할 일:** 수정한 `index.html`과 `delete-data.html`을 GitHub Pages 저장소에 다시 업로드(commit)해야 실제 URL에 반영됩니다. 시크릿 창으로 URL을 열어 시행일이 바뀌었는지 확인하세요. Play 콘솔의 URL은 그대로이므로 다시 입력할 필요는 없습니다.
